pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/BanielChips/CEN4802c-Final-Project.git'
            }
        }

        stage('Build') {
            steps {
                script {
                    sh 'mvn clean install -DskipTests=true'
                }
            }
        }

        stage('Test') {
            steps {
                script {
                    sh 'mvn test'
                }
            }
        }

        stage('Package') {
            steps {
                script {
                    sh 'mvn package'
                }
            }
        }

        stage('Run Application') {
            steps {
                bat 'mvn spring-boot:run'
            }
        }
    }

    post {
        always {
            echo 'Cleaning up...'
        }
        success {
            echo 'Pipeline completed successfully!'
        }
        failure {
            echo 'Pipeline failed!'
        }
    }
}
